# activity-generator
first install node.js in your device , go to the project folder where this files have forked and type node init to intialize the node package in the terminal or command prompt and then type npm install express axiom ejs body-parser.
then after that go to the package.json and create a  key "type": "module",and then run the index.js using the command  node index.js in terminal.
